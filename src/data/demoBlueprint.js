// ─── DEMO BLUEPRINT ───
// Static pre-generated Blueprint for landing page preview.
// Project: SaaS Task Manager — universally relatable, shows off all features.

export const DEMO_CONFIG = {
    domain: "saas",
    projectName: "TaskFlow Pro",
    mission: "A modern SaaS task management platform with real-time collaboration, Kanban boards, sprint planning, time tracking, team workspaces, role-based access control, and integrations with Slack, GitHub, and Google Calendar. Designed for agile teams of 5-50 people.",
    langConvo: "en",
    langCode: "en",
    stack: {
        framework: "Next.js 15",
        api: "tRPC",
        orm: "Prisma + PostgreSQL",
        auth: "NextAuth.js",
        billing: "Stripe",
        hosting: "Vercel",
    },
    priorities: ["performance", "security", "maintainability", "scalability"],
    rigor: "balanced",
};

export const DEMO_GENERATED = {
    rules: {
        output: `# TaskFlow Pro — Workspace Rules (rules.md)
# Generated by Blueprint Compiler v2.1 | IDE: Cursor

## 1. Identity & Scope
You are a senior full-stack architect specializing in SaaS platforms.
You are building **TaskFlow Pro** — a real-time task management SaaS.
Your tech stack is Next.js 15 + tRPC + Prisma + PostgreSQL + Stripe.

## 2. Architecture Principles
- **App Router only** — no pages/ directory, all routes in app/
- **Server Components by default** — "use client" only when needed
- **Colocation** — components, hooks, and utils live next to their routes
- **Feature folders** — src/features/{feature}/components|hooks|utils|types
- **Barrel exports forbidden** — direct imports only (tree-shaking)

## 3. Database Conventions
- All tables use \`snake_case\` names and \`uuid\` primary keys
- Every table has \`created_at\` and \`updated_at\` timestamps
- Soft delete via \`deleted_at\` column (nullable)
- Multi-tenant: every query MUST include \`workspace_id\` filter
- Indexes on all foreign keys + frequently filtered columns

## 4. API Layer (tRPC)
- Routers in \`src/server/routers/{resource}.ts\`
- Input validation via Zod schemas (co-located)
- Procedures: \`query\`, \`mutation\` — never mix side effects in queries
- Auth middleware: \`protectedProcedure\` for all authenticated routes
- Rate limiting: 100 req/min per user, 1000 req/min per workspace

## 5. Authentication & Authorization
- NextAuth.js with Google + GitHub + email/password providers
- Session strategy: JWT (not database sessions)
- RBAC: owner > admin > member > viewer
- Permission checks at tRPC middleware level, NOT in components
- Invite system: email-based with 7-day expiry tokens

## 6. Security Baseline
- OWASP Top 10 compliance mandatory
- All user input sanitized via DOMPurify + Zod
- CSRF protection via Next.js built-in
- Content Security Policy headers in middleware.ts
- No secrets in client bundles — use NEXT_PUBLIC_ prefix only for public values

## 7. Real-time Architecture
- WebSocket via Socket.io for board updates + notifications
- Optimistic updates on the client, server reconciliation
- Presence system: show active users on each board
- Debounce: 300ms for rapid updates (drag & drop)

## 8. Testing Strategy
- Unit tests: Vitest for utilities and hooks (80%+ coverage)
- Integration tests: tRPC router tests with test database
- E2E tests: Playwright for critical flows (auth, create task, payment)
- No snapshot tests — they provide false confidence

## 9. Performance Targets
- LCP < 2.5s, FID < 100ms, CLS < 0.1
- Bundle size: < 150KB initial JS (gzipped)
- Database queries: < 50ms p95, max 3 queries per page load
- ISR for marketing pages, SSR for dashboard

## 10. Error Handling
- Global error boundary at app layout level
- tRPC errors: structured { code, message, field? } format
- Sentry integration for production error tracking
- User-facing errors: friendly messages, never expose stack traces

## 11. Code Style
- TypeScript strict mode — no \`any\`, no \`@ts-ignore\`
- Functional components only — no class components
- Custom hooks for all shared logic (useTask, useBoard, useWorkspace)
- Constants in SCREAMING_SNAKE_CASE, functions in camelCase

## 12. Git Discipline
- feat:, fix:, refactor:, docs:, test:, chore: prefixes
- One commit = one logical change
- Branch: feat/feature-name, fix/bug-name
- PR requires 1 approval + passing CI

## 13. Billing & Subscription
- Stripe Checkout for initial signup
- Stripe Customer Portal for self-serve plan changes
- Webhook handler at /api/webhooks/stripe
- Plans: Free (5 boards), Pro ($12/mo, unlimited), Team ($29/seat/mo)
- Grace period: 7 days after failed payment before downgrade`,
        quality: { score: 91, feedback: "Comprehensive rules with clear conventions." },
    },

    skills: {
        output: `# TaskFlow Pro — Skills & Competencies

## Skill: Database Migration
### When to Use
When adding, modifying, or removing database tables/columns.

### Procedure
1. Create migration: \`npx prisma migrate dev --name descriptive_name\`
2. Update \`schema.prisma\` with changes
3. Generate client: \`npx prisma generate\`
4. Seed if needed: \`npx prisma db seed\`
5. Test migration rollback: \`npx prisma migrate reset\` (dev only)

### Constraints
- Never modify existing migrations — always create new ones
- Name format: \`YYYYMMDD_description\` (e.g., \`20260217_add_time_tracking\`)
- Always include DOWN migration logic

---

## Skill: Stripe Integration
### When to Use
When implementing payment flows, subscription management, or webhook handling.

### Templates
\`\`\`typescript
// Webhook handler pattern
export async function POST(req: Request) {
  const body = await req.text();
  const sig = req.headers.get("stripe-signature")!;
  const event = stripe.webhooks.constructEvent(body, sig, WEBHOOK_SECRET);
  
  switch (event.type) {
    case "checkout.session.completed":
      await handleCheckoutComplete(event.data.object);
      break;
    case "customer.subscription.updated":
      await handleSubscriptionUpdate(event.data.object);
      break;
    case "invoice.payment_failed":
      await handlePaymentFailed(event.data.object);
      break;
  }
  return Response.json({ received: true });
}
\`\`\`

### Constraints
- Always verify webhook signatures
- Idempotency: handle duplicate events gracefully
- Log all webhook events for debugging

---

## Skill: Real-time Board Updates
### When to Use
When implementing drag-and-drop, live cursor, or collaborative editing.

### Architecture
- Socket.io rooms = one room per board
- Events: \`task:move\`, \`task:update\`, \`user:join\`, \`user:leave\`
- Optimistic UI: update client immediately, reconcile on server ACK
- Conflict resolution: last-write-wins with timestamp check

### Constraints
- Max 50 concurrent users per board
- Reconnection: exponential backoff (1s, 2s, 4s, 8s, max 30s)
- Heartbeat: 15s interval, 30s timeout

---

## Skill: Component Architecture
### When to Use
When creating new UI components for the dashboard.

### Template
\`\`\`tsx
// Feature component pattern
"use client";
import { useMemo } from "react";
import { api } from "~/trpc/react";

interface TaskCardProps {
  taskId: string;
  onUpdate?: (task: Task) => void;
}

export function TaskCard({ taskId, onUpdate }: TaskCardProps) {
  const { data: task, isLoading } = api.task.byId.useQuery({ id: taskId });
  
  if (isLoading) return <TaskCardSkeleton />;
  if (!task) return null;
  
  return (
    <div className="task-card" data-priority={task.priority}>
      {/* ... */}
    </div>
  );
}
\`\`\`

### Constraints
- Every component must have a loading skeleton
- Props interface must be explicitly typed (no inline)
- Interactive components need \`data-testid\` for Playwright

---

## Skill: API Endpoint Design
### When to Use
When adding new tRPC procedures or REST endpoints.

### Naming Convention
- \`resource.list\` — paginated list with filters
- \`resource.byId\` — single item by ID
- \`resource.create\` — create with validation
- \`resource.update\` — partial update (PATCH semantics)
- \`resource.delete\` — soft delete (set deleted_at)

### Constraints
- Max 50 items per page (configurable up to 100)
- Cursor-based pagination for large datasets
- Always return total count for UI pagination`,
        quality: { score: 88, feedback: "Well-structured skills with practical templates." },
    },

    workflows: {
        output: `# TaskFlow Pro — Workflows

## Workflow: New Feature Development
### Description
Step-by-step process for implementing a new feature end-to-end.

### Steps
1. **Create branch**: \`git checkout -b feat/feature-name\`
2. **Database first**: Add Prisma schema changes + migration
3. **API layer**: Create tRPC router with Zod validation
4. **UI components**: Build with loading states + error boundaries
5. **Tests**: Write integration tests for API + E2E for critical paths
6. **Documentation**: Update API docs + inline JSDoc
7. **PR**: Create with description template, request review
8. **Deploy**: Preview deployment on Vercel, verify in staging

### Checkpoints
- After step 2: Run \`prisma migrate dev\` — must succeed
- After step 3: Run \`pnpm test:api\` — all router tests pass
- After step 5: Coverage must be ≥ 80% on new code
- After step 8: Smoke test all critical flows in preview URL

---

## Workflow: Production Deployment
### Description
Checklist for deploying to production safely.

### Pre-Deploy
1. All CI checks passing (lint, types, tests)
2. Database migrations reviewed and tested
3. Environment variables verified in Vercel dashboard
4. Stripe webhook endpoints configured for production
5. Sentry release created with source maps

### Deploy
1. Merge PR to \`main\` → auto-deploy to Vercel
2. Monitor Vercel deployment logs for errors
3. Verify health endpoint: \`GET /api/health\`
4. Run E2E smoke tests against production URL
5. Check Sentry for new errors (wait 10 min)

### Post-Deploy
1. Update changelog in GitHub release
2. Notify team in Slack #releases channel
3. Monitor metrics dashboard for 30 minutes
4. If rollback needed: revert commit + redeploy`,
        quality: { score: 86, feedback: "Clear, practical workflows with checkpoints." },
    },

    context: {
        output: `# TaskFlow Pro — Architecture Decision Records

## ADR-001: Next.js 15 App Router
### Status: Accepted
### Context
We need a React framework that supports SSR, ISR, and API routes with excellent DX.
### Decision
Use Next.js 15 with App Router (not Pages Router).
### Rationale
- App Router enables React Server Components → smaller client bundles
- Built-in layouts and loading states reduce boilerplate
- Streaming SSR improves perceived performance
- Middleware for auth checks at the edge
### Trade-offs
- Steeper learning curve vs Pages Router
- Some libraries not yet compatible with RSC
- Server/client boundary requires careful management

## ADR-002: tRPC over REST
### Status: Accepted
### Context
We need a type-safe API layer between frontend and backend.
### Decision
Use tRPC instead of traditional REST or GraphQL.
### Rationale
- End-to-end type safety without code generation
- Automatic validation with Zod schemas
- No API documentation drift — types ARE the contract
- Excellent React Query integration built-in
### Trade-offs
- Tied to TypeScript ecosystem
- Not suitable for public APIs (no OpenAPI spec)
- Third-party integrations need REST endpoints anyway

## ADR-003: Prisma + PostgreSQL
### Status: Accepted
### Context
We need a reliable database with a type-safe ORM for rapid development.
### Decision
Use Prisma ORM with PostgreSQL (Supabase-hosted).
### Rationale
- Prisma schema as single source of truth for database types
- Auto-generated migrations with rollback support
- PostgreSQL: JSONB for flexible task metadata, full-text search
- Connection pooling via PgBouncer for serverless environments

## ADR-004: Multi-Tenancy via workspace_id
### Status: Accepted
### Context
Multiple teams/organizations must have isolated data.
### Decision
Shared database with \`workspace_id\` column on all tenant-scoped tables.
### Rationale
- Simpler than database-per-tenant at our scale (< 10K workspaces)
- Row-Level Security (RLS) at Prisma middleware level
- Easy cross-workspace features (marketplace, templates) in the future
### Constraints
- EVERY query must filter by workspace_id — enforced via Prisma middleware
- Never expose workspace_id in client URLs (use slugs)

## ADR-005: Stripe for Billing
### Status: Accepted
### Context
We need subscription management with multiple plans and per-seat pricing.
### Decision
Use Stripe Checkout + Customer Portal + Webhooks.
### Rationale
- Industry standard, handles SCA/PSD2 compliance
- Customer Portal eliminates custom billing UI
- Metered billing support for future usage-based pricing
- Excellent webhook system for event-driven architecture`,
        quality: { score: 92, feedback: "Excellent ADRs with clear rationale and trade-offs." },
    },

    prompt: {
        output: `# TaskFlow Pro — Project Overview (PROMPT_START)

## Quick Start
\`\`\`bash
git clone https://github.com/team/taskflow-pro
cd taskflow-pro
pnpm install
cp .env.example .env.local
npx prisma migrate dev
pnpm dev
\`\`\`

## Project Structure
\`\`\`
taskflow-pro/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── (auth)/             # Auth routes (login, register)
│   │   ├── (dashboard)/        # Protected dashboard routes
│   │   │   ├── boards/         # Kanban board views
│   │   │   ├── tasks/          # Task detail views
│   │   │   ├── settings/       # Workspace settings
│   │   │   └── billing/        # Subscription management
│   │   ├── api/                # API routes
│   │   │   ├── trpc/           # tRPC handler
│   │   │   └── webhooks/       # Stripe webhooks
│   │   └── layout.tsx          # Root layout
│   ├── features/               # Feature modules
│   │   ├── boards/             # Board feature
│   │   ├── tasks/              # Task feature
│   │   ├── workspace/          # Workspace feature
│   │   └── billing/            # Billing feature
│   ├── server/                 # Server-side code
│   │   ├── routers/            # tRPC routers
│   │   ├── db.ts               # Prisma client
│   │   └── auth.ts             # NextAuth config
│   ├── components/             # Shared UI components
│   ├── hooks/                  # Shared hooks
│   ├── lib/                    # Utility functions
│   └── types/                  # Global type definitions
├── prisma/
│   ├── schema.prisma           # Database schema
│   ├── migrations/             # SQL migrations
│   └── seed.ts                 # Dev seed data
├── tests/
│   ├── unit/                   # Vitest unit tests
│   ├── integration/            # API integration tests
│   └── e2e/                    # Playwright E2E tests
├── .cursor/rules/              # Cursor IDE rules
├── next.config.ts
├── tailwind.config.ts
└── package.json
\`\`\`

## Key Commands
| Command | Description |
|---------|-------------|
| \`pnpm dev\` | Start dev server (localhost:3000) |
| \`pnpm build\` | Production build |
| \`pnpm test\` | Run all tests |
| \`pnpm test:e2e\` | Run Playwright E2E |
| \`pnpm db:push\` | Push schema to database |
| \`pnpm db:studio\` | Open Prisma Studio |

## Environment Variables
\`\`\`env
DATABASE_URL=postgresql://...
NEXTAUTH_SECRET=...
NEXTAUTH_URL=http://localhost:3000
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...
\`\`\``,
        quality: { score: 89, feedback: "Clear project overview with practical quick start." },
    },
};
